#include "main.h"

/**
 * _puts_recursion - puts chars recursively
 * @s: chars to print
 * Return: void
 */

void _puts_recursion(char *s)
{
	if (!*s)
		_putchar('\n');
		return;
	if
		_putchar(*s);
		_puts_recursion(s + 1);
}
