#include "main.h"

/**
 * _print_rev_recursion - print chars in a string in rev recursively
 * @s: pointer to string
 * Return: void
 */

void _print_rev_recursion(char *s)
{
	if (!*s)
		return;
	if
		_print_rev_recursion(s + 1);
		_putchar(*s);
}
